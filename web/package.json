{
  "name": "demo-web",
  "version": "1.0.0",
  "description": "Clean Web Frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Running Web unit tests... PASSED\" && exit 0",
    "lint": "echo \"Running Web linter... PASSED (0 errors)\" && exit 0",
    "build": "node build.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
